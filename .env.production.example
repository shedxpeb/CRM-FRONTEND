# Copy to .env.production and set real production values.
# Next.js loads .env.production when NODE_ENV=production.

NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_CAPABILITIES_PATH=/system/capabilities

# Server-side only — never expose backend origin to the browser
BACKEND_URL=https://api.your-domain.com

IMAGE_HOSTNAME=api.your-domain.com
